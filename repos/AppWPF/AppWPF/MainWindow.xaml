<Window x:Class="AppWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <DataGrid HorizontalAlignment="Center" Height="318" Margin="0,106,0,0" VerticalAlignment="Top" Width="780"
                  ItemsSource="{Binding Alunos}"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  >
            <DataGrid.Columns>
     
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel HorizontalAlignment="Center" Height="110" Margin="0,10,0,0" VerticalAlignment="Top" Width="780">
            <Button Content="Adicionar" CommandParameter="{Binding}" Command="{Binding Novo}" Height="21" Width="63" HorizontalAlignment="Left" Margin="10,0,0,10"/>
            <Button Content="Editar" CommandParameter="{Binding}" Command="{Binding Editar}" Height="21" Width="63" HorizontalAlignment="Left" Margin="10,0,0,10"/>
            <Button Content="Excluir" CommandParameter="{Binding}" Command="{Binding Deletar}" Height="21" Width="63" HorizontalAlignment="Left" Margin="10,0,0,0"/>
        </StackPanel>

    </Grid>
</Window>
